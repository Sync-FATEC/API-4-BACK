name: CI Deploy

on:
  push:
    branches:
      - preprod

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Deploy via SSH to EC2
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.KEY }}
        script: |
          cd /home/ec2-user/API-4-BACK
          git config --global --add safe.directory /home/ec2-user/API-4-BACK
          sudo chown -R $USER:$USER /home/ec2-user/API-4-BACK
          git fetch
          git pull
          npm install
          npm run dev > /home/ec2-user/API-4-BACK/serve.log 2>&1 &